apiVersion: run.tanzu.vmware.com/v1alpha3
kind: TanzuKubernetesCluster
metadata:
  # Changed name to avoid conflict and satisfy DNS rules (start with a letter)
  name: tkg-294 
  namespace: vns
spec:
  topology:
    controlPlane:
      replicas: 1
      vmClass: best-effort-xsmall
      storageClass: storageclass
      tkr:  
        reference:
          # Updated to match the 1.29.4 legacy TKR name from your list
          name: v1.29.4---vmware.3-fips.1-tkg.1
    nodePools:
      - name: node-pool-1
        replicas: 1
        vmClass: best-effort-xsmall
        storageClass: storageclass
        tkr:
          reference:
            name: v1.29.4---vmware.3-fips.1-tkg.1