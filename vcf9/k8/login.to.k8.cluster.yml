#!/bin/bash

# Configuration
VSPHERE_SERVER="10.10.10.1"
VSPHERE_USER="prj1admin@vsphere.local"
CLUSTER_NAME="vns1cls12"
NAMESPACE="vns1"


kubectl vsphere login --server="$VSPHERE_SERVER" --vsphere-username "$VSPHERE_USER" --tanzu-kubernetes-cluster-name "$CLUSTER_NAME" --tanzu-kubernetes-cluster-namespace "$NAMESPACE" --insecure-skip-tls-verify
